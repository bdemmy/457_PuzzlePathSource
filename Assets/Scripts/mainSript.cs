using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mainSript : MonoBehaviour
{   
    /*
     This script is going to be the controller of the game, from start to finnish the general plot of 
     the game runs through here and reacts to player input to update levels depending on time and  the 
     actions taken by the player.
     */
    // Start initializes all traps and level features sets them to the default properties given at the start of the game.
    void Start()
    {
        
    }

    // Update function checks to see what the player is has done and if a certain action is taken to that
    // would change the game then update calls the function that would change the game in the corresponding way.
    void Update()
    {
        
    }

    // function opens a specific door or wall object that is passed to it
    // or in other words it sets the color to clear and makes it passthroughable
    void openDoor()
    {


    }

    // function checks which torch is touch interacted with and flips the opposite two 
    // torches correspondingly, if all torches are lit, final door is opened.
    void torchPuzzle() {


    }

    // function checks which painting is touch interacted with and displays  
    // the information so the player can get the chest puzzle clues.
    void paintingPuzzle()
    {

    }
}
